import { CONFIG_ENVIRONMENTS } from './enviroments.config';
import { authOptions } from './auth/auth.config';
import { authorizeConfig } from './auth/functions/authorize-function.config';

export { CONFIG_ENVIRONMENTS, authOptions, authorizeConfig };
